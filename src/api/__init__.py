"""API module for exposing NVR functionality."""

from typing import Any


def start_server(config: Any) -> None:
    """Start the API server.

    This is a placeholder for whatever web framework will be used
    (e.g., Flask, FastAPI).
    """
    print("API server starting with config:", config)
